export const validateStudent = (_payload) => {
  return { valid: false, error: 'Not implemented — use Copilot to scaffold validateStudent()' }
}

export const validateClass = (_payload) => {
  return { valid: false, error: 'Not implemented — use Copilot to scaffold validateClass()' }
}
